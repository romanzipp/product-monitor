# Secrets only. All other configuration lives in config.yaml
# (see config.example.yaml). Copy to .env and fill in your Pushover credentials.
PUSHOVER_TOKEN=your-pushover-application-token
PUSHOVER_USER=your-pushover-user-or-group-key
